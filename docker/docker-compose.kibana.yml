services:
  kibana:
    image: "docker.elastic.co/kibana/kibana:${ES_VERSION}"
    container_name: "kibana"
    depends_on:
      - elasticsearch
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
      - XPACK_SECURITY_ENABLED=${ES_SECURITY}
    ports:
      - "5601:5601"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:5601/api/status | grep -q '\"state\":\"green\"' || exit 1"]
      interval: 15s
      timeout: 10s
      retries: 40
      start_period: 40s
